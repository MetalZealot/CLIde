# Grayson's TODO

Conventions: `- [ ]` open, `- [~]` partly done, `- [x]` done (move to `docs/todo-done.md`
once verified — and keep those entries short: commit messages + ADRs are the canonical record).
`[x]` means the work is implemented and merged, which is not the same as live-verified on
3001; an archived entry that hasn't had its live check yet says so in its own text, so read
the entry rather than the checkbox.
Size guesses: **S** = small/frontend-only, **M** = medium, **L** = large/needs design, **?** = unknown until investigated.
Screenshots for many of these live in `UI Visual References/` (repo root, untracked — only present in the main checkout).
Tracked in-repo since 2026-07-22 so concurrent sessions share one board: when a session
picks up an item on a topic branch, tag the item `— in progress on <branch>` and commit,
so other sessions see the claim.

## Provider maintenance

- [~] **Codex App Server is now the default interactive Chat transport — verification of the rollout remains.** Implementation merged to `main` in `cbf2960`: `resolveCodexChatTransport` (`codex-chat-transport-state.ts`) returns `app-server` unless `CLIDE_CODEX_CHAT_TRANSPORT=sdk`, non-interactive agent jobs stay on the TypeScript SDK, capabilities downgrade after a startup fallback, and configured-vs-actual transport is exposed in diagnostics. Per `docs/maps/2026-07-25-codex-chat-transport-architecture.md` the outstanding work is verification, not code: protocol drift, recovery, concurrency, Pi resource use, and the installed-PWA rollout matrix. **L**
- [~] **Establish a recurring provider SDK/CLI update process — living-map structure in progress on `docs/clide-provider-map`.** Current provider truth now belongs under `docs/maps/`: the canonical CLIde capability map maps normalized behavior to provider/runtime bindings, provider-native maps retain exhaustive surface discovery, and compact ledgers record release decisions. Dated investigations remain in `specs/`; generated audit artifacts remain temporary. The Codex process checks official docs plus the OpenAI GitHub release, tag comparison, tagged SDK/App Server source, installed declarations/help/features, and generated default/experimental bindings; it classifies changes as consumed contract, mapped opportunity, compatibility watch, or no action. Exact SDK/bundled-CLI pinning, diagnostics, and the smoke matrix remain required. The Claude map was refreshed 2026-07-30 against SDK 0.3.165 and runtime 2.1.220 and now has its own ledger; unlike Codex, Claude's SDK and spawned runtime are deliberately unpinned, so a version-pair diagnostic is the first follow-up. Still open: create Cursor/OpenCode maps after artifact-level audits, automate from/to reports, add redacted unknown-method/item diagnostics, and make the typed capability registry generate or validate the canonical map. **M recurring**
- [x] **Integrate upstream v1.37.0** — merged (`658d536`), built, and deployed to 3001 on 2026-08-04. Full record: [the archived handoff spec](specs/archive/2026-08-01-post-v1-37-main-divergence-and-merge-handoff.md). **Everything still outstanding from it — the unpushed `main`, the three commits with static coverage only, the `chore/upstream-1.37` branch/worktree, the two remaining ADR reassessments, and the five queued specs — now lives in [Post-v1.37 remaining work](specs/2026-08-04-post-v1-37-remaining-work.md).** Moved to `todo-done.md`. **L**
- [ ] **Assess upstream 1.37's worktree foundations (deferred out of the 1.37 merge).** Grayson's call 2026-07-29: land the other phases first, assess this separately; reaffirmed 2026-07-30 after a full read of the tag. Upstream ships 19 server files under `server/modules/worktrees/` (5 services, routes, module, index, **8 test files ~35 KB**) + `useWorktreesController` + New/Merge/Remove modals + `WorktreesView`, registered as a fourth Git tab. **None of it is in the merged tree** — start the assessment from the immutable `v1.37.0` tag. Its product contract is unsafe as-is: modal defaults are squash + remove-after-merge + delete-branch, and worse, `worktree-merge.service.ts` **hardcodes `deleteBranch: true`** in post-merge cleanup — no checkbox controls it, so the source branch dies unconditionally whenever removal is on, via a `git branch -D` in a swallowed `try/catch` (`worktree-remove.service.ts:61`); `backdrop-blur-sm` in all four modals (ADR 0001). Also: `countAheadBehind` never compares against a remote-tracking ref (cannot tell *unmerged* from *unpushed*), the porcelain parser strips `refs/heads/`, the base branch is implicitly `entries[0].branch`, nothing reports agent/runtime occupancy, and merge rewrites the serving checkout with no self-hosting guard. The `listWorktrees` descriptor and the test harness are the parts genuinely worth harvesting. **Start here: [the archived integration spec's Review 2026-07-30 + harvest table](specs/archive/2026-07-29-upstream-1-37-integration.md#review-2026-07-30-what-upstream-ships-and-what-to-keep)** — file-by-file take/amend guidance. Requires the identity + preflight contract from [`docs/specs/2026-07-26-git-source-control-workspace-ux.md`](specs/2026-07-26-git-source-control-workspace-ux.md) and ADR 0016's grouping. **L**
- [~] **Per-session model state is durable now; the *new-session default* still isn't.** The sidecar half closed with [ADR 0025](decisions/0025-session-model-picks-live-in-the-database.md) — per-session picks live on the `sessions` row and the JSON file is out of the code path. What remains: the picker's new-session default is still `localStorage` per provider (`claude-model`, …, `useChatProviderState.ts:96`), so it does not follow the user across browsers or devices. Any fix must keep ADR 0003's precedence — a stored value must never outrank transcript evidence. **M**
- [ ] **Composer menu follow-ups from upstream 1.37 (deferred, worth taking later).** In benefit-per-risk order: (a) adopt `useComposerMenuAnchor` and point CLIde's inline effort dropdown at it — CLIde duplicates ~60 lines of the same portal/Escape/outside-click/re-anchor logic but positions with `left`/`top`; upstream's `right`/`bottom` anchoring avoids the first-frame mispaint and its `maxWidth` stops phone overflow (**S**, ADR-neutral); (b) replace the permission-mode **cycling button** with upstream's `ComposerPermissionMenu` — blind cycling through 4–5 modes is poor on touch, and CLIde already computes the per-mode descriptions it can only show one of (**S/M**); (c) `ComposerModelMenu` only as a *replacement* decision for the `/models` popup, never alongside it — needs an ADR 0003 review, since two model surfaces is worse than either (**M/L**).

## Bugs

- [ ] **Paste is still image-only, so you can't paste a PDF or a text file into the composer. [upstreamable]** `handlePaste` (`useChatComposerState.ts`, the `item.type.startsWith('image/')` filter and the `clipboardData.files` fallback just below it) predates v1.37 and was never widened when upstream `06e7ee9` (#1037) dropped the dropzone's `accept` map. Drag-drop and the attachment picker take any file; paste silently drops everything that isn't an image — no error, no attachment. Both filters have to go, and `handleAttachmentFiles` already validates size/count so nothing else needs to change. Found 2026-08-04 while investigating the Android attachment chooser (ADR 0026). **S**

- [ ] **The permission-mode picker's "Default" is a lie — it sends no mode at all, so you silently get whatever `settings.json` says. [upstreamable]** `claude-runtime.provider.js:190` only forwards a mode when it isn't `'default'`, so picking Default leaves `sdkOptions.permissionMode` unset and the SDK falls through to `~/.claude/settings.json` → `permissions.defaultMode`. Grayson's is `acceptEdits`, so "Default" has been running as acceptEdits with nothing in the UI saying so — which is why default mode felt like auto-approve (found 2026-08-04 while hunting for a tool-approval prompt to test `9a9d47b`). Two other layers compound it and are *not* bugs, just invisible: allowlist rules (`.claude/settings.local.json` here carries `Bash(git *)` and `Bash(npm run *)`, which is nearly every command run in this repo) and Bash sandboxing. Fix is a choice, not a patch: either send `permissionMode: 'default'` explicitly and mean it, or relabel to something honest ("Inherit from settings") and surface the resolved mode. **Wider job this belongs to: "default" means three different things across the Claude CLI, the Agent SDK, and CLIde's picker, and nothing documents how the layers resolve** — that terminology/IA pass is the real deliverable; this entry is its concrete first defect. Escape hatch worth keeping in mind while testing: `TOOLS_REQUIRING_INTERACTION` (`AskUserQuestion`, `ExitPlanMode`) is checked *before* the bypass/allow/deny block, so those two always prompt regardless of mode or allowlist. Provider-agnostic picker surface → check the other adapters before changing shared UI. **M**
- [ ] **Aborting the *first* message of a new session orphans it into two sidebar rows.** Found live 2026-08-03 by Grayson while verifying the v1.37 abort fixes; **this is a fourth, distinct id-mapping defect — not one of the three ADR 0013 bugs and not ADR 0008's rotation case.** Symptom: abort a brand-new session's first message very early; nothing looks wrong immediately, then a second row appears once the provider gets around to writing its transcript. The original row is stuck on the blank "Continue your conversation" screen forever, and the row with your actual message is a different session. Reproduced rows: app `23ceb635-9094-4fea-acea-6519a8a8a959` (`provider_session_id` NULL, `jsonl_path` NULL, created `03:26:36`) and watcher-created `1a94cddc-95b5-42c4-b326-0d156c64cdca` (`session_id` == `provider_session_id`, "Untitled Claude Session", created `03:26:39`); the transcript holds the prompt plus `[Request interrupted by user]`.
  - **Mechanism.** `capturedSessionId` is only ever assigned *inside* the stream loop — `claude-runtime.provider.js:832`, `for await (const message of queryInstance)`. An abort that trips the AbortController before the SDK yields its first message runs **zero iterations**, so `capturedSessionId` stays null, no `session_created` is emitted, `ws.setSessionId` never fires and **`assignProviderSessionId` is never called at all**. Meanwhile the CLI subprocess has already created the jsonl and written the turn, so the synchronizer correctly sees an unclaimed transcript and indexes it as a new session.
  - **The reconciliation already exists and simply never runs.** `assignProviderSessionId` (`sessions.db.ts:210`) explicitly merges a watcher-created duplicate into the app row — transcript path and name adopted, duplicate deleted, all in one transaction so the sidebar never shows both. There is even coverage: `sessions-provider-mapping.test.ts:72`. The race is handled; the *call* is what goes missing. So this is a missing-trigger bug, not a missing-mechanism bug.
  - **Pre-allocating the id is not available.** Checked `sdk.d.ts`: `query()` takes `resume: sessionId` for an existing session only, and `forkSession` *returns* a new UUID. There is no caller-supplied session id for a new query, so the mid-stream announcement is unavoidable and the fix has to be reconciliation on teardown.
  - **Fix shape.** On run teardown where `capturedSessionId` is still null, look in the run's project transcript dir for a jsonl created within the run's lifetime that no session row claims and no alias tombstones, and call `assignProviderSessionId(appSessionId, discoveredId)` — the existing merge then collapses the duplicate whether or not the watcher has indexed it yet. Disambiguate by matching the transcript's first `user` row against the aborted prompt; **if more than one candidate matches, do nothing** — that leaves today's behavior, so an ambiguous case is no worse than the status quo. Needs a test with the two ids deliberately unequal, in the spirit of `chat-session-addressing.test.ts`. **Careful tier** — touches session mapping; back up `~/.cloudcli/auth.db` first. **M**
  - **Stray artifacts from the repro are still live** in the sidebar and on disk (both rows above + `~/.claude/projects/-home-gnuthall-Projects-cloudcli-wt-upstream-1-37/1a94cddc-….jsonl`). Delete the transcript before the DB rows when cleaning up (FS before DB).
- [ ] **Cursor: the composer's permission-mode picker is mostly cosmetic.** The capability matrix exposes Default / Accept Edits / Bypass / Plan, but `spawnCursor` never reads `permissionMode`; only the separate `toolsSettings.skipPermissions` value adds `-f`. Current Cursor CLI supports `--mode=plan`, and `--force` is the write/auto-approval control, so the adapter needs an explicit mapping (or the capability matrix should stop advertising modes it does not implement). The new long-press mode popup reports the current no-op honestly rather than overstating protection. **S/M**
- [ ] **Composer attachments silently reject files that are too large or the wrong type.** `useDropzone` defines no `onDropRejected`, and `imageErrors` only renders on accepted attachment cards, so an over-5MB or unsupported file appears to do nothing. Add clear rejection feedback without regressing the native Android picker path. [upstreamable] **S**
- [ ] Convo window: clicking the mode selector on desktop shifts the UI and buttons in the message box. **S**
- [ ] File Editor: long lines don't wrap — they push the left edge into the conversation box and squish it narrower. (sometimes?) Should wrap by default. **S/M**
- [~] **Convo window: scroll-up pagination is glitchy + has a "dead roof" — the accepted improvement is merged (`12ede24`) and the `fix/chat-scroll-pagination` branch/worktree were retired 2026-08-04; two residual symptoms remain, see "Accepted improvement" below. Two failed fix attempts 2026-07-23, reverted to baseline (unchanged). (Blocks/informs the "map" sidebar feature below.)** Three facets: (a) **jitter** — screen jitters while older messages load on scroll-up; (b) **dead roof** — *some* sessions just won't load more on scroll-up, seemingly random, sometimes cleared by leaving the session and coming back; (c) **UX ask** — when a load fires at the top the viewport gets pushed/scrolled down and you lose your place; ideally it stays put. Root causes found (all in `useChatSessionState.ts` unless noted):
  - **Dead roof** = `handleScroll` only releases `topLoadLockRef` when `scrollTop > 20`, but the scroll-restore holds position by pushing the viewport down by *exactly the height the new page added*. A short page — or one that's all `tool_result` rows (they collapse into their `tool_use`, adding ≈0 visible height) — never pushes `scrollTop` past 20px, so the lock stays `true` forever, parked at the roof with `hasMore` still true. ("Leave and come back fixes it" = the session-change effect resets the lock.)
  - **Jitter** = native browser scroll-anchoring (`overflow-anchor: auto`) *and* the manual restore `useLayoutEffect` both compensate for the prepend → double-correction bounce.
  - Store side is **sound**: `fetchMore` (`useSessionStore.ts`) always makes progress while `hasMore`; the `offset` (counts `tool_result` rows) vs `total` (excludes them) mismatch is **display-only** ("Showing X of Y"), not a loading bug.
  - **Why my two fixes failed:** attempt 2 *removed* the lock so loads gate only on in-flight + near-top — but then every `onWheel`/`onTouchMove` at the top fired a load, and each load's restore nudges down. On real touch (many rapid `touchmove` events per drag) that's a machine-gun of tiny loads all pushing down = "jitters and keeps pushing down, locked in place." **Critical lesson:** the headless Playwright harness (`.scroll-repro.mjs`, discrete 1500px wheel flings every 250ms) does **not** model real touch and gave a false PASS on that regression. **Any future fix must be validated by a hands-on touch test on the actual PWA (build:client → refresh 3001), not the harness.**
  - **Correct-fix shape for next time:** keep baseline's *one-load-per-arrival* intent, but replace the `scrollTop>20` lock-release with something that (i) does **not** depend on `scrollTop` crossing a fixed pixel threshold (that's the dead-roof), and (ii) does **not** re-fire per `touchmove`. Candidates: delta-based re-arm (measure movement away from where the load happened), a short debounce/one-load-per-N-ms gate, or arming on a discrete "reached top" edge rather than a level. Pair with `overflow-anchor: none` on the pane so the single manual restore in the tick-keyed `useLayoutEffect` is authoritative. Symbols: `loadOlderMessages`, `handleScroll`, `topLoadLockRef`, `pendingScrollRestoreRef`, the restore `useLayoutEffect`, the append-anchor effect near `scrollPositionRef`; container in `ChatMessagesPane.tsx` (`onWheel`/`onTouchMove` both = `handleScroll`, so gestures fire even pinned at `scrollTop=0`). Distinct from upstream #1050 (render-side streaming/idle jank) — this is the data/pagination side; complementary. Leftover: `.scroll-repro.mjs` (untracked, repo root) + `/tmp/clide-repro-token` (expires) can be deleted. **M/?**
  - **Implementation on `fix/chat-scroll-pagination` (2026-07-31; revised after three branch-test passes, awaiting re-test):** edge-triggered top loading re-arms only after a prepend creates real scroll distance; collapsed/no-height pages chain in a controlled sequence; undersized initial pages proactively fill because they cannot emit a top scroll event; a fetch-progress check avoids locking on a failed/no-op request; and a tick guarantees restore even when normalized message count does not change. The pane disables native scroll anchoring and estimated `content-visibility` geometry; one passive rAF-throttled `scroll` listener replaces the scroll/wheel/touch trio; and the two transient/legacy load-all surfaces are consolidated into one inline history control. **First live result:** Firefox became materially smoother, but session entry could still land above the bottom and prepends still shifted slightly. Follow-up replaced the three-stable-frame timing guess with a content `ResizeObserver`, anchored prepends to the first visible message through late layout, reused fresh per-session cache on A → B → A navigation, and kept reconnect/watcher refreshes inside the currently loaded pagination window instead of replacing 20 rows with the full transcript. **Second live result:** entry/baseline loading became more reliable, but some sessions still reached a roof with no header and in-flight user scrolling still snapped when the captured request-start anchor restored. Next correction removes the hook's duplicated `hasMoreMessages`/`allMessagesLoadedRef` pagination authority in favour of `SessionStore.hasMore`, keeps the history control visible whenever the store says older rows exist, and continuously updates the captured anchor target as the user scrolls during the request. App-level `chat.ping`/`chat_pong` does not reach message listeners; only an actual reconnect refreshes history. Follow-up measurement found 20-row history requests for the four largest sampled 1.5–2.9 MB transcripts complete locally in 66–181 ms, ruling out full-file normalization as the multi-second spinner; the remaining watcher effect was replaying the same nonzero `externalMessageUpdate` on later project/session object and processing-state changes, so each watcher version is now session-scoped and consumed once. **Third live result:** loading is substantially smoother and reliable in Chromium and Firefox, with only an occasional small shift and a one-frame flash of content from above. The store was notifying React about prepended rows before the hook armed the anchor and expanded its visible window, allowing an intermediate commit; those updates are now staged before the store notification so the layout restore runs before first paint. Installed-mode safe-area rules change the app's static rectangle but do not mutate during pagination; PWA still needs its own final pass. **Remaining:** hands-on branch-test touch + Firefox re-test, then installed-PWA pass on 3001; do not merge before Grayson confirms those.
  - **Accepted improvement (2026-08-01):** Grayson confirmed the combined branch is considerably smoother than the previous Chromium/Firefox/PWA experience and worth integrating. The final narrow correction moved the content `ResizeObserver` anchor adjustment out of `requestAnimationFrame` and into its pre-paint callback. **Deferred residual:** the assistant provider logo/header can still flash briefly, and reaching the roof while a page loads can still move the viewport. Keep this item `[~]` and revisit those symptoms separately; do not describe pagination as fully fixed.
- [ ] **Source Control: a failed commit is completely silent — the panel just "won't commit" with no reason shown. [upstreamable]** Hit 2026-07-27 trying to commit `docs/specs/archive/2026-07-26-files-multi-selection-and-organization.md` from the panel, repeatedly; the same commit succeeded first try from the CLI (`62b7b54`). Every failure path swallows the error: `commitChanges` (`useGitPanelController.ts` ~639) `console.error`s and returns `false` on both a non-2xx response and a thrown fetch, and `handleCommit` (`CommitComposer.tsx` ~42) only clears the message box on success — so the UI is indistinguishable from "nothing happened". Ruled out: the file wasn't gitignored, and the server route itself is fine — `POST /api/git/commit` (`server/routes/git.js` ~616) stages then runs `git commit` in the repo root, and a probe commit under the systemd service's exact minimal env (nvm PATH, no login shell) passed both husky hooks, so it isn't a hook-can't-find-`lint-staged`/`commitlint` PATH problem. **Leading suspect:** the route `git add`s each file *before* committing, and the file was left **staged but uncommitted** in the index — exactly the state a `git commit` rejected by the `commit-msg` hook leaves behind. This repo runs commitlint/`config-conventional`, so any non-conventional message the user types (e.g. "added files spec") makes the hook exit non-zero → 500 → silently discarded, forever, no matter how many times you press Commit. Fix: surface `data.error` from the commit response in the composer (hook output included — it says exactly what's wrong), and treat a rejected commit as a distinct visible state rather than a no-op. Bonus: pre-validate against commitlint, or at least hint the conventional-commit format in the panel. Generic surface → any forker with commit hooks hits this. Upstream not yet searched. **S**
- [ ] Sidebar session names sometimes don't match the names shown in the terminal under `claude /resume`. **?**
- [ ] Shell view: no touch-drag scrolling — view is pinned to the bottom, can't scroll up through output. **M**
- [ ] **Git panel branch switcher can churn/wreck the working tree when the selected project is CloudCLI's *own* checkout — likely-common case, worth a guard. [upstreamable]** The switcher runs a real `git checkout` on the project's working dir (`handleSwitchBranch` in `GitPanelHeader.tsx` → `switchBranch` in `useGitPanelController` → server switch route in `server/routes/git.js`; also `requestSwitch` in `BranchesView.tsx`). When that project *is* the directory the app runs from — the norm for anyone forking CloudCLI to hack on CloudCLI — switching branches rewrites the whole working tree under the running dev server / built service. Observed 2026-07-21: `main` ↔ `feat/add-hermes-provider` = **178 files**; under the Vite dev server it triggered a mass reload → blank/stuck white screen; with uncommitted changes the checkout also silently carries them or fails. Extra footgun: switching to a *remote* branch passes the stripped name to `git checkout`, auto-creating a local tracking branch. Fix ideas: (a) detect when the target repo == the dir the app itself runs from (server cwd / repo root) and warn hard or block; (b) harden the switch confirmation — actually **refuse** when the working tree is dirty (checkout would fail/carry anyway) with a clear message, instead of today's soft "make sure you have no uncommitted changes" text; (c) minimum: stronger warning copy. **Upstream check 2026-07-21:** `gh issue list --repo siteboon/claudecodeui --search "branch switch checkout"` → none; `gh pr list --search "branch checkout"` → only #535 (git-panel redesign, merged) and #578 (worktree toggle, draft), neither addresses this. Generic git-panel surface → affects all forkers. **M** *(surfaced 2026-07-21 while testing the branch-dropdown grouping change; my own switcher tap on this repo triggered it)*
- [ ] Duplicate-session double-send: pressing send twice on a brand-new chat creates two sessions running the same message (observed 2026-07-16 ~18:47, CLIde sessions `3b59479f`/`04f37c34` → Claude JSONLs `097539ba`/`fc07019e`, identical first message 250ms apart). Mechanics (code-verified): `handleSubmit` in `src/components/chat/hooks/useChatComposerState.ts` awaits `POST /api/providers/sessions` (line ~770) before doing *anything* visible — the optimistic message append, processing state, input clear, and `chat.send` all happen after the POST resolves, and there's no in-flight/re-entry guard; the send button stays enabled (`ChatComposer.tsx:578` only checks `!input.trim()` when not loading). So if the POST round-trip stalls (Pi busy / slow network), the UI looks like the tap did nothing, the text is still in the box, and a second press re-runs the whole branch with `currentSessionId` still null → second POST → second session, then both queued `chat.send`s spawn together. Fix: add an `isSubmittingRef` guard around `handleSubmit` (drop or queue re-entrant calls), and give instant feedback (disable send + optimistic append) *before* the awaits. **S** *(found 2026-07-17 investigating the duplicate)*

- [ ] **Project force-delete leaves subagent transcripts orphaned on disk** (surfaced 2026-07-23 alongside `0a738ae`). Force-delete unlinks each session's *top-level* `<slug>/<session-id>.jsonl` (those are the paths on the session rows), but a session's nested `<slug>/<session-id>/subagents/agent-*.jsonl` files were never session rows — the synchronizer skips them — so they're never removed. The new empty-dir prune correctly refuses to touch them (non-recursive `rmdir` → `ENOTEMPTY` is caught), so the whole `<slug>/` tree survives. Pre-existing, not caused by `0a738ae`, which only made the preservation explicit. Decision needed: should force-delete purge a deleted session's subagent transcripts too (they belong to the project being deleted), or is leaving them the safer default? If yes, walk each deleted session's `<session-id>/` dir rather than blanket-recursive-deleting `<slug>/`. Ties to the subagent items under Features. **S — decision first, small fix after**

- [ ] **Browser MCP hardening: snapshot-first, reference-based automation.** Build on the DTO/result-boundary fix above: replace body-text/selector/coordinate-first interaction with bounded accessibility-oriented snapshots and short-lived opaque element refs; keep the Browser panel's independent preview; mark page content untrusted; add stale-ref-safe actions, redacted errors, and real-client provider compatibility checks. Full deferred design and rollout/test plan: `docs/specs/2026-07-29-browser-mcp-hardening.md`. **L — design complete; implementation after the current DTO fix lands and is independently verified**

## Mobile UX polish

- [ ] **Mobile bottom navbar — move the top tab strip to a bottom nav (design decided 2026-07-22, see ADR 0005).** Supersedes the old "top bar: plugin buttons cramp the conversation title" item — the answer is: core tabs (Chat / Files / Git / plugins) go in a compact icon-only bottom navbar; the freed top bar keeps sidebar-toggle / title+project / new-chat / kebab (per mockup `UI Visual References/CLIde UI.svg`, untracked). **This intentionally re-adds what upstream removed in `a8dab0e` (PR #632)** — their removal was about the implementation (`fixed bottom-0` + `pb-mobile-nav` compensation padding everywhere + dual nav paradigms), not the pattern. Implementation constraints, settled:
  - **In normal flow** — last row of the app's flex column, never `position: fixed`; zero compensation padding anywhere (this neutralizes upstream's entire removal rationale).
  - **Hide when the keyboard opens** (composer focused) — upstream's old `isInputFocused → translate-y-full` slide-away is the right behavior; `env(safe-area-inset-bottom)` under it.
  - **Mobile breakpoint only** — desktop keeps current nav.
  - **Plugin/extensible tabs = shared surface**: registry of `{ id, icon, panel, isAvailable(provider) }` so provider-specific tabs (Browser, Taskmaster, community plugins) hide rather than break under Cursor/Codex/OpenCode; cap ~5 visible slots, overflow into a "More" menu (upstream's deleted `MobileNav.tsx` did exactly this — `git show a8dab0e^:src/components/app/MobileNav.tsx` is a free 179-line reference).
  - Active-state: tint + dot (or label-on-active-only) — the plug icon is the most ambiguous of the four.
  - Keyboard/safe-area behavior is **only verifiable on the installed PWA against 3001**, not the 5173 tab.
  - Rebase hazard: permanent divergence near `AppContent.tsx` (upstream's removal site) — expect conflicts there on future rebases.
  Plan: build on a topic branch in a worktree (`git worktree add ../cloudcli-wt-bottom-nav -b feat/bottom-nav main`) — claim this item with the branch name when work starts. **L**
- [ ] UI-revamp kebab (3-dot) menu: include "Copy session ID" for the **current** session (the long-press sidebar menu covers other sessions since 2026-07-22, see `docs/todo-done.md` — but copying the id of the chat you're *in* still requires finding it in the sidebar). Slot it into the top-bar kebab from ADR 0005's layout. **S — part of the bottom-navbar/top-bar rework** *(consider surfacing the provider/JSONL id here too if the sqlite mapping hop proves annoying — would need a small endpoint or field, see `docs/todo-done.md`)*
  - **Also rehome upstream 1.37's chat export here (decided 2026-08-03 — keep it floating until then).** `ChatExportMenu` arrived with v1.37 as a `sticky right-4 top-3 z-10` download button pinned over the transcript's top-right (`ChatMessagesPane.tsx:157`), which on the S20 sits over the first message. The export *writers* are fine and worth keeping — `src/components/chat/utils/chatExport.ts` (223 lines, markdown/html/pdf). It's the 77-line presentation shell that needs replacing, and it breaks three fork conventions, so don't port it as-is: (1) **ADR 0009** — it dismisses on `click`, which 0009 explicitly rejected because a scroll gesture can finish with the menu still floating; route it through `ContextMenuOverlay` like every other menu. (2) **ADR 0010** — its backdrop is a bare `fixed inset-0` (`ChatExportMenu.tsx:73`), so the global `body.pwa-mode .fixed.inset-0` rule shifts its origin down by the header padding and the catcher misses the top strip; a tap up there won't dismiss it *in the installed PWA only*. (3) **No i18n** — "Export chat" / "Export as:" are hardcoded English while the rest of the fork goes through `t()`. **S — folds into this item**
- [ ] General condensing/shrinking of UI elements and popup menus on mobile — some assets/text get cut off due to size. **M — grab-bag, itemize as found**
- [~] Sidebar, Projects Tab: For Projects that contain a session that is currently active could also have an indicator of some sort rhat there is an active session inside. Perhaps same green dot as active session indicator. **Partly done by the sidebar-indicator redesign (see `docs/todo-done.md`, 2026-07-18):** collapsed project rows now get an **amber border** when any *loaded* child session needs action (pending permission/question). The old time-based "active" green was retired in favour of a read-aware model. Remaining follow-ups: (a) project rows now show **amber (needs-action)** and **green (unread)**; **grey (running)** at the project level is still not surfaced — add if wanted; (b) project-level attention/unread only sees *paginated-in* sessions — an unloaded child that needs action won't light the collapsed project until expanded (would need project-id carried on the attention signal). **S**
- [ ] Sidebar: needs-action (amber) can stick if a background session's pending permission is answered in **another client** without ever opening the session here — it only clears on open or on a `permission_cancelled` frame. Acceptable for now (opening clears it); revisit if it proves annoying. **S**
- [ ] Tool-call copy button placement on mobile — the copy control (`OneLineDisplay.tsx` `renderCopyButton`, plus `BashCommandDisplay`) is now always-visible on touch (Samsung fix `05b176b`, 2026-07-21), but it spans the whole right edge of the tool row, which is heavy. Rework how it's displayed on mobile (compact/inline, or fold into a row action) as part of the planned UI revamp — the hover-reveal is still the right behaviour to keep on desktop. **S/M — design decision first**
- [ ] **Git branch picker — label branches by remote (origin/ vs upstream/), grouped per-remote like VS Code/Zed. [Tier 2]** Ties into the later UI revamp. **Tier 1 is DONE (2026-07-21, working tree, uncommitted):** the compact header dropdown (`GitPanelHeader` — `renderBranchOption` + `DropdownSectionHeader`) now groups **Local vs Remote** with section headers + a globe icon for remotes, matching what `BranchesView` (`SectionHeader`/`BranchRow`, `isRemote`) already did; `GitPanel` now feeds it the `localBranches`/`remoteBranches` split. What's left (Tier 2) is showing **which remote** a branch belongs to. Root blocker: the server (`server/routes/git.js`, the branch-list builder that filters `remotes/` and strips the `remotes/<remote>/` prefix, then dedupes remote names against locals) throws away the remote identity before it reaches the client — so today the UI can only tell local-vs-remote, not origin-vs-upstream. Scope: (a) `git.js` — preserve the remote name (return remote branches with prefix, or as `{ remote, name }`); (b) shared branch shape in `git-panel/types/types.ts` (`localBranches`/`remoteBranches`); (c) both views group/label per-remote (`BranchesView`, `GitPanelHeader`); (d) the switch-branch call path — `git checkout` of a remote-tracking branch needs correct handling (create local tracking branch), don't pass a bare stripped name. Provider-neutral surface (generic git, not Claude-specific) — stays fine for all providers. **M**

## Model picker follow-ups (2026-07-13 review + 2026-07-16 upstream-comparison review — this section is the complete outstanding model-picker list)

- [ ] **#8 — PRIORITY: live verification of the per-session model stack (`8771eea` + `5d9da84`), still pending.** Build + restart precondition met 2026-07-22 (service runs HEAD) — just run the tests: (a) leak test — Session A on Fable, send, confirm reply model in JSONL; open Session B, pick Haiku, don't send; back to Session A, `/model` popup should still show Fable; send, confirm JSONL says Fable not Haiku. (b) stale-pick resume test — pick model X in popup, change model via Shell `/model` or `/fast`, send from web chat, confirm the newer choice wins (recency gate) not X. (c) fresh-session popup/header sync (`6535092`, display-only). (d) smoke-test a Codex session if configured (its config.toml model must no longer masquerade as session state). **S (test time only)**
- [ ] **#15 — `currentProviderModel` is a second, unmaintained copy of the session's model** (found 2026-08-04 mapping the picker's surfaces). `useChatProviderState.ts:611` reads `sessionModel ?? providerModels[provider]`, and its comment claims "switching sessions shows (and sends) what each session actually runs with" — **that comment is false.** `sessionModel` has exactly one writer, `setSessionModel` inside `selectProviderModel` (~:605), fired only on an explicit popup pick. It is never populated from the session and **never cleared on session switch**. The store's `slot.model` — which *is* the session's real model, fetched on every switch — is read in exactly one place, `useChatComposerState.ts:889`, at send time. Two visible consequences: (a) **the effort dropdown reads the wrong model** — `currentProviderEffortOptions` and `reconcileStoredEffort` both key off `currentProviderModel`, so a session running Haiku can be offered another model's effort values (likely the same surface as the Haiku effort-picker gap in `docs/upstream-candidates.md`); (b) **fork sends a stale model** — `useChatComposerState.ts:527` posts `model: currentProviderModel` to `/fork`, so a forked session can be seeded with the last-picked model rather than the one the forked session was actually running. The send path is unaffected (it reads `slot.model` directly) and the `/models` popup is unaffected (server-authoritative via `resolveSessionModel`). Fix shape: derive `currentProviderModel` from the store slot, or drop `sessionModel` entirely — one authority, not two. **S**
- [ ] #2 — Shell /model stdout regex over-captures: picking "Default" in the Shell makes the ACTIVE MODEL box show the raw sentence "Default (recommended)" (no card highlight) until the next turn. The `(.+?)\.?$` capture in claude-models.provider.ts grabs the whole trailing sentence; 'fable'/'opus' only resolve by substring luck. Fix: anchor/tighten the capture. **S** ***Escalated by `8771eea` (found in 2026-07-16 upstream-comparison review): the over-captured string is no longer display-only — transcript-sourced models now feed the send path (client `fetchModel` adopts `source: 'transcript'` into the session slot; server `resolveResumeModel` falls back to the transcript model). After Shell `/model` → Default, the next web-chat send can pass literal "Default (recommended)" as the SDK model → broken turn. Fix the regex AND validate transcript-sourced models before they're sendable (only adopt values that alias-resolve to a catalog option or look like a real model id).*** *2026-07-17: this risk is now proven real — a 529 bricked a live session by feeding the transcript placeholder `<synthetic>` into the send path (fixed narrowly in `422411f`, see `docs/todo-done.md`). The general validation half of this item is still open; the `/^<.*>$/` guard only covers Claude's placeholder convention, not arbitrary junk like "Default (recommended)".*
- [ ] #3 — 1M-context picks via Shell map to the non-1M alias: resolveClaudeModelAlias only recognizes the literal '[1m]' token, so "Sonnet 4.5 (1M context)" highlights plain Sonnet until the next turn. Fix: also recognize "(1M context)" phrasing. **S**
- [ ] #4 — getCurrentActiveModel reads+parses the entire session JSONL (4.5MB for a long session) on every /models open, even when a fresh pick wins anyway. Fix: stat the file and skip the read when the pick's updatedAt is newer than file mtime, or read only the tail. **S/M** *Got more valuable with `8771eea`: the same parse now also runs on session switches (30s client TTL) and on resume-model fallbacks. Sub-detail: the 30s client TTL only applies when `slot.model` is non-null, so sessions running the default model re-parse on every switch.*
- [ ] #7 — client-side race: a `fetchModel` GET in flight when the user makes a popup pick resolves *after* `setModel`, clobbering the optimistic slot value (possibly to null). Display-only in practice — the server's pick-recency gate still resolves the right model on the next send — but the popup/slot can briefly show stale state. Fix: drop fetch responses issued before the last `setModel` (sequence counter, like `_fetchSeq`). **S** *(found in 2026-07-16 review)*
- [ ] #9 — decide the "Default" pick semantics: since `8771eea`, a session adopts its transcript's concrete model alias from turn 2 onward, so picking "Default" (popup or new-session picker) survives literally as `default` for only one turn before pinning to e.g. `sonnet`. Consequence: existing sessions no longer follow later changes to settings.json's default. Mostly desirable ("a session keeps its model") but it silently reinterprets the user's "Default" choice — decide whether `default`-seeded sessions should stay floating (e.g. don't adopt transcript models into the slot when the seed was `default`), or keep and document current behavior. **S/M — decision first**
- [ ] #10 — housekeeping: `pickSupersedesTranscript` lives in `claude-models.provider.ts` but is imported by the shared `provider-models.service.ts` — a Claude module leaking into provider-agnostic code (the function itself is generic). Move it to `server/shared/utils.ts`. **S**
- [ ] #11 — upstreaming opportunity: upstream issue #981 ("Can not change model from Chat UI") and open PR #996 ("resolve per-session model overrides using app session ID") hit the same bug family our `85ddd7e`/`5d9da84`/`8771eea` stack fixes. Consider PRing the stack (or parts) to siteboon/claudecodeui once verified (#8); either way, **watch #996 — if it merges, the next rebase will conflict across `provider-models.service.ts`, `claude-sdk.js`, and `commands.js`**. **M (PR) / awareness (rebase)**
- [x] #12 — **context-window ceiling is now model-derived** (2026-07-26, `73214c5`). New resolver `server/modules/providers/list/claude/claude-context-window.ts` implements Claude Code's own decoded algorithm — registry window, capped by env `CLAUDE_CODE_AUTO_COMPACT_WINDOW` → settings.json `autoCompactWindow` → a 200000 `model-default` clamp that only applies below a 1e6 window, then `usable = window − min(maxOutputTokens, 20000)`. Model facts (`context.window`, `max_output_tokens.default`, 1M support) are mirrored from the SDK registry (`@anthropic-ai/claude-agent-sdk` 0.3.220 `sdk.mjs`), which exports no runtime accessor for them — refresh the table on every SDK bump. `normalizeClaudeModelId` handles picker aliases, canonical ids, dated wire ids (`claude-opus-4-1-20250805`), Bedrock/Vertex/regional ids, the `[1m]` suffix, and entries with no `context` block. All three synced token paths wired: `extractTokenBudget` reads the model off the assistant frame and, on the `modelUsage` branch, prefers the SDK's own `contextWindow`/`maxOutputTokens` (so a model newer than the table still resolves); `/token-usage` takes the model off the same transcript row it already scanned for usage and overlays a newer popup pick via `pickSupersedesTranscript` — deliberately *not* a `getCurrentActiveModel` call, which would have added the second full-JSONL parse #4 complains about; `extractHistoryTokenUsage` reads its row's model. `CONTEXT_WINDOW` is kept as an absolute operator override. Measured on a real Opus 5 session: 121,711 tokens now reads ~12% of a ~1M ceiling instead of 76% of 160,000. **Superseded in part on 2026-07-27 (`0267856`): the SDK's own `getContextUsage()` disagreed with this derivation — 200K models are 200,000 (no reply reserve) and 1M models 967,000 — so the numbers here were corrected and the SDK reading now takes precedence where available.** 12 new tests (130 provider tests total; the one `claude-rewind.test.ts` failure is pre-existing on `main`), typecheck, lint zero errors. Codex untouched — `extractCodexContextTokenUsage` already reads `model_context_window`. **Server-side — needs `npm run build:server` + an SSH restart to go live; not yet live-verified.** **M**
- [ ] #13 — **add pinned legacy models to the Claude picker** (Opus 4.8/4.7/4.6, Sonnet 4.6/4.5, Haiku 4.5-pinned) so a session can be pinned to a specific generation instead of a floating alias that silently re-points on every launch. Design brief: [`docs/specs/2026-07-26-pinned-legacy-model-catalog.md`](specs/2026-07-26-pinned-legacy-model-catalog.md). Key findings: Claude Code's own `/model` picker offers **only** aliases, so there's no upstream UI to copy; the SDK/CLI does accept concrete ids, so the runtime supports it. The blocking work is **not** the list — `resolveClaudeModelAlias`'s substring matching will mis-resolve pinned/dated ids onto alias cards (a transcript naming `claude-opus-4-1-20250805` would highlight "Opus 5" and, per #2's escalation, can *resume* as Opus 5), so it must become an exact/longest-match resolver first; fold #3's "(1M context)" phrasing fix into the same pass. Effort blocks must be gated on each model's registry `capabilities` (Opus 4.6 has `max` but no `xhigh`; Sonnet 4.5/Haiku 4.5 have none) — same helper would fix #6's Default-card residual. **#12 is done**: reuse its `normalizeClaudeModelId` / `CLAUDE_MODEL_CONTEXT_SPECS` (`claude-context-window.ts`) — the exact/longest-match id normalization and the per-model registry table this item needs already exist there. **M** *(spec'd 2026-07-26, implementation deferred to a fresh session)*

## Shell sync

- [ ] Shell view shows a stale transcript snapshot and Disconnect/Restart don't reliably refresh it (observed 2026-07-16). Symptom: shell pane showed the conversation as of an earlier point; pressing Disconnect then reconnecting, and pressing Restart, both still showed the old content; it only caught up after something else refreshed. Actual mechanics (code-verified): the Shell tab is a separate `claude --resume <id>` CLI in a server-side PTY — it renders the transcript as of process start and never live-tails turns made in the web chat, so some staleness between restarts is inherent. The two real defects: (1) **Disconnect can never refresh** — client `disconnectFromShell` just closes the WS; the server keeps the PTY in `ptySessionsMap` and on reconnect replays up to 5000 buffered chunks ("[Reconnected to existing session]"), i.e. the same stale view by design (`shell-websocket.service.ts` ~281-308). (2) **Restart resumes a possibly-stale id** — forceRestart kills the PTY and spawns `claude --resume <provider_session_id>`, where the id comes from the sessions DB row for the *UI-selected* session (`server/index.js:128-135`); if the sidebar's selectedSession.id or the DB mapping lags the live session chain (session ids rotate per SDK turn), the fresh CLI resumes an older JSONL fork → still stale. Note: the client-side session handling Haiku suspected is NOT the bug — refs are synced (`useShellRuntime.ts:34-40`) and a disconnect-on-session-change effect exists (`useShellRuntime.ts:103-110`). Fix direction: on forceRestart, resolve the *latest* session id server-side (from the transcript chain / most recent JSONL for that session lineage) instead of trusting the client's possibly-stale id; consider surfacing "resumed at <time>, may be behind" in the shell header. Related side-effect to keep in mind: commands run in the Shell (e.g. `/model`) write into the same session transcript the web chat uses. **M/L** *2026-07-17 follow-up (investigated whether chat↔shell swapping can duplicate sessions — it can't, see notes): (a) correction — "session ids rotate per SDK turn" appears wrong on current versions: all 30 transcripts in the cloudcli project are one-file-per-conversation with stable ids, no forks, so Restart's stale-id risk is lower than written; (b) **defect (3), the reverse direction**: the chat view deliberately ignores `session_upserted` (`useChatRealtimeHandlers.ts:163` — sidebar-only), so turns written from the Shell (e.g. `/model`, or full prompts run in the PTY) never appear in an open chat view until you switch away and back (message refetch happens only on session open). Shell→chat staleness + chat→shell staleness are both display-level; the underlying transcript is a single shared file, so no duplicate sessions arise from tab swapping.*

## Theming

- [ ] **Typography overhaul** — replace Encode Sans + Merriweather (Google Fonts CDN) with self-hosted Figtree (UI + prose) + Iosevka (code). Decisions are settled and locked in `docs/specs/2026-07-20-typography-system.md` (which supersedes the claude.ai handoff doc at repo root): Merriweather removed deliberately, Shell/xterm untouched, current code-wrap behavior kept, `--font-prose` variable reserved for a future Settings font-switcher. Family swap first, then tune the type scale live in the dev server — scale sizes are targets, not commitments. Part of the larger UI overhaul. **M**
- [ ] Color picker for accent colour; maybe an option for a slight hue shift on the background. Light/dark variants. **M**
- [ ] Optional presets matching model-provider branding (Anthropic, OpenAI, Google). **S once the picker exists**

## Features (bigger ideas)

- [ ] **Register CLIde as a Web Share Target — the only way to get a native file-attach flow on Android.** Established 2026-08-04 (ADR 0026): the composer's attachment control is at the ceiling of what `accept` can do, because Chrome for Android narrows the file picker *only* when `accept` is a single recognised media family. An in-app source menu was built and reverted the same day — it could not change the outcome, only add a tap in front of it. Eleven variants were probed on the installed PWA; ten produced an identical wide-open chooser (two camera tiles, document browser hidden behind a "Photos & videos" label), and only `accept="image/*"` behaved differently. So the Files row can never be made to open a clean document picker — **do not retry this with a better MIME list.** A share target inverts the flow instead: `share_target` in `public/manifest.json` (`method: POST`, `enctype: multipart/form-data`, `params.files`) plus a server route that accepts the POST and hands the file to a composer session, so you attach by opening My Files/Gallery → Share → CLIde, with no OS chooser at all. Open questions: which session a shared file lands in (last active? a picker? a new session?), whether the existing `attachmentUpload` route (`server/modules/assets/assets.routes.ts`, no `fileFilter`, 10 MB × 10) can serve the POST directly or needs its own unauthenticated-but-tokened entry, and how the SPA is woken with the payload. PWA-only surface, so **verifiable only on the installed app against 3001**, never 5173. Provider-agnostic (it ends at the composer's attachment list, which every provider shares). **L — needs design first**

- [ ] **Add an opt-in diagnostic flight recorder under Settings → System.** Preserve the successful Samsung attachment-investigation method as a permanent, local-only, bounded, redacted, behavior-neutral mode with Copy/Clear, visible recording status, early-boot URL activation, and a small baseline of PWA, service-worker, auth, WebSocket, navigation, app-boundary, and user-action events. Full design, safety boundary, phased implementation, and live device acceptance plan: `docs/specs/2026-08-01-opt-in-diagnostics-flight-recorder.md`. **Do not start until the upstream v1.37 integration is complete, live-verified, and merged; re-audit all target paths afterward in a fresh topic worktree. M — design complete; implementation deferred**
- [ ] **Claude Code settings are almost entirely unreachable from CLIde.** Audited 2026-07-28 in `docs/specs/2026-07-28-claude-code-settings-surface-audit.md` (inventory recovered from the 2.1.220 binary's JSON-Schema strings + the SDK's `Settings` type, not docs — `/config` is an in-session command with no CLI verb). Claude Code has **~140 settings-cascade keys**; `/config` exposes ~50 of them, and CLIde exposes **zero**. Key reframe: the plumbing already exists — `claude-sdk.js:233` sets `settingSources = ['project','user','local']`, so `~/.claude/settings.json` is *already in force* in every CLIde session; what's missing is any way to see or edit it. Three findings that matter: (1) **two parallel permission systems** — Settings → Agents → Claude → Permissions writes `localStorage['claude-settings']` → `sdkOptions.allowedTools`, while `permissions.allow`/`deny` from settings.json applies simultaneously, neither visible from the other; (2) CLIde **silently overrides** `model`, `effortLevel`, `permissions.defaultMode` and `env`, so those must be read-only in any UI or it will lie; (3) `~/.claude.json` globalConfig (~45 keys: `editorMode`, `diffTool`, `autoConnectIde`, …) is CLI-process-local and **never reaches a CLIde session** — porting it would be decorative. Two SDK APIs make this cheap, both verified live: `resolveSettings()` (exported at runtime, `@alpha`) returns effective values **plus per-key provenance and the full tier list** using the CLI's own merge engine, and `Options.settings` accepts an inline object in the highest user-controlled tier for per-session/per-project overrides with no disk writes. Tiering: ~25 keys worth adapting (permissions, thinking, auto-compact, fast mode, memory, skills, workflows, artifacts, attribution, worktree, retention), ~15 read-only, ~45 terminal-only to **never** surface (many already have CLIde-native equivalents — a second dead switch is the main design hazard), ~35 enterprise plumbing out of scope. Recommendation: ship the **read-only cascade viewer first (S/M)** — it derisks the `@alpha` API, needs no write path, and makes the permissions split visible. Lands *after* the Settings revamp below (whose stated non-goal is "no new settings"), inside its per-provider screen as a provider-slotted feature, not a global tab. **M (Tier A) / L (with hooks + sandbox, which need their own specs)**
- [~] **Source Control: manage Git worktrees and integrate branches without leaving CLIde.** Design completed in `docs/specs/2026-07-26-git-source-control-workspace-ux.md`; implementation remains. **Updated 2026-07-27 (see ADR 0016):** the external `CLIde_Source_Control_Git_Workspace_UX_Reference.md` was merged into that spec and then deleted (it's the only surviving copy of that material now). The merge adds the project → repository → checkout identity model (CLIde's project *is* a checkout today, so worktrees become unrelated top-level projects — grouping by `git rev-parse --git-common-dir` is what makes "one project, all its worktrees, all its agents" possible, and it **must be decided before the sidebar revamp** because it changes the sidebar's data shape), a Phase 0 of truthfulness fixes (conflicts are folded into `modified` and unreachable by the API; no merge/rebase state detection exists; detached HEAD renders as a branch called `HEAD`; commit failures are silent), integration-operation naming (no ambiguous "Apply"), safe-removal copy, iconography/colour/accessibility rules, and per-turn diffs gated on provider capability rather than built as a new snapshot system. The current Git panel can switch branches but cannot list/create/remove worktrees or merge a completed topic branch into its target, so agent-driven work still depends on an agent or an external terminal for basic workspace lifecycle operations. The design defines a guarded repository → worktree → branch → remote model, structured ref identities, workspace/runtime occupancy, worktree create/remove flows, and source→target integration with preflight, fast-forward/merge policy, conflict reporting, and separate verify/push/PR/cleanup actions. Build this on the existing branch-switcher safety and remote-label items; refuse unsafe operations on dirty trees, protect the checkout serving CLIde, and never delete a branch or worktree with unmerged work by default. **L — implementation**
- [ ] **True session syncing?** Using Claude Code directly does not list CloudCLI conversations. **? — needs investigation first: where does each store sessions?**
- [~] **Codex plan usage: dynamic limit windows, credit/reset balance, and account activity — merged into `main` (`25952ea`), awaiting production restart verification.** Codex implements `IProviderUsage` through the bundled CLI app-server's `account/rateLimits/read` and `account/usage/read` methods; provider-neutral types preserve Claude spend/cap behaviour while supporting Codex balance, resets, and activity statistics. Settings, `/cost`, and the composer badge are provider-driven, and API-key auth cleanly reports unsupported. Verified against the real Codex account in an isolated dev instance, plus provider tests, typechecks, lint (zero errors), and client/server production builds. Remaining before Done: restart CLIde and confirm the installed app on port 3001. The per-session context ring remains a separate concern. **M/L**
- [ ] **Subagent / spawned-agent tracking in the UI** (CLIde has nothing like the CLI's per-session agent view). What exists today (investigated 2026-07-23): Claude writes subagent transcripts to `~/.claude/projects/<slug>/<session-id>/subagents/agent-<id>.jsonl`; the synchronizer *deliberately* skips them (`isSubagentTranscript`, `claude-session-synchronizer.provider.ts:39`) so a spawned agent never becomes its own sidebar session. Within a session, subagent tool calls are grouped under the parent via `parent_tool_use_id` → attached as `subagentTools` on the message (`claude-sessions.provider.ts`), which is *display grouping*, not agent tracking. Gap: no "this session spawned N agents; here's each one's activity/status/tokens" surface. Investigation-first: decide what to surface (live running-agent indicator? expandable per-agent transcript?) and whether it reads the `subagents/` dir or the in-stream sidechain frames. Codex has an analogous `isSubagentSessionMeta` skip, so design provider-agnostically. **L — needs investigation first**
- [ ] **Does token/usage tracking count subagent tokens?** Checked 2026-07-23 — answer is *two different systems, two different behaviors*: (a) **Plan-window % + credits/cost** (`/cost`, `claude-usage.provider.ts`) is fetched live from Anthropic's OAuth usage endpoint, so it's authoritative and **already includes** agent tokens (Anthropic bills them) — not locally summed, no undercount. (b) The **per-session context ring** (`extractTokenBudget`/`extractHistoryTokenUsage`/`/token-usage`) scans the transcript and **skips `isSidechain` subagent rows** by design (comment: "Subagent transcripts carry their own, smaller context") — which is *correct* for a context-window gauge since subagents run in a separate window, but means a session that spawns heavy agents shows a low ring while real plan usage climbs (a perception gap, not a billing bug). Open question: confirm those semantics are intended, and consider surfacing subagent token consumption somewhere (per-agent, or a session-total that includes sidechains) so the gap isn't surprising. Ties to the subagent-tracking item above. **S/M — mostly a decision + small surface**
- [ ] **Compaction is inherited but unsurfaced — no `/compact`, no auto-compact signal, no boundary marker.** Investigated 2026-07-27. What already works: `server/claude-sdk.js` sets `settingSources = ['project', 'user', 'local']`, so the SDK reads `~/.claude/settings.json` including `autoCompactEnabled` / `autoCompactWindow` — i.e. a CLIde session inherits the CLI's auto-compact behaviour rather than running uncapped; and CLIde already *renders* compaction summaries (`isCompactSummary` rows are parsed in `claude-sessions.provider.ts`, exempted from the hidden-row guard, and given their own branch in `MessageComponent.tsx`). What's missing: (1) no way to trigger a compact from the composer (the CLI's `/compact`, including `/compact <instructions>`); (2) the SDK's `SDKCompactBoundaryMessage` (`type: 'system'`, `subtype: 'compact_boundary'`, `compact_metadata.trigger: 'manual' | 'auto'`, `pre_tokens`/`post_tokens`) is not handled anywhere, so when a compact *does* fire mid-turn the UI shows no boundary and the ring just silently drops; (3) no indication of where auto-compact will fire (see the `getContextUsage()` item below — `autoCompactThreshold` + `isAutoCompactEnabled` come free with it). Evidence that this is real and not theoretical: the local transcripts contain 14 compaction events, all `trigger: "manual"` from the terminal CLI, one at **274,501 pre-tokens** — a session cannot pass 200k with auto-compact on unless the window really is 1M, which corroborates `#12`'s model-derived ceiling. **Not verified:** whether auto-compact has ever actually *fired* inside a CLIde session (0 of the 14 were `auto`, and `autoCompactEnabled` is unset in settings.json, so it rests on Claude Code's documented default rather than an observation here) — worth confirming before building UI that promises it. Provider-agnostic surface: Codex/Cursor/OpenCode have their own (or no) compaction concept, so a shared `/compact` affordance must be capability-gated per adapter, like `/rewind` was. **M — needs the auto-compact-fires confirmation first** **Two rendering defects split off and fixed 2026-07-30 (see commit):** (a) *duplicate `/compact` bubble* — Claude writes `/compact` to the transcript twice, as the user's real prompt row (the rewind anchor) and again ~50ms later as the `<command-name>` wrapper, so both normalized into identical user bubbles and only the first could be rewound to. Measured across every transcript on the Pi: **18/18 `/compact` invocations duplicate, 0/31 other local commands do** (`/model`, `/plugin`, `/agents`, `/config` write *only* the wrapper — which is why wrappers are rendered at all), so the fix is a general echo-drop in `fetchHistory`, not a `/compact` special case. (b) *referenced files were invisible in Chat* — compaction appends `attachment` rows (`compact_file_reference`, and `file` for re-read contents, both carrying `displayPath`) that `normalizeMessage` ignored entirely; they now ride on the summary row as `compactReferences` and render as a muted "Referenced: …" line under the collapsed summary, mirroring Codex's `memoryCitations`. Still open here: the composer `/compact` affordance, `SDKCompactBoundaryMessage` handling, and the auto-compact threshold signal.
- [ ] **`/context`: use `gridRows` for a closer visual match to the CLI's panel.** The SDK's breakdown carries the CLI's own square-grid rendering (`gridRows`); CLIde parses it away and rebuilds the picture as a stacked bar. Left over from the context work archived in [`docs/todo-done.md`](todo-done.md) (2026-07-27). **S**
- [ ] **`/usage`: per-model cost breakdown, like the CLI's own `/usage`.** Claude Code's `/usage` is primarily a *price* view: plan bars, then a "This session" line (Cost, Active time, Opus share, Cache hit %), then a `Breakdown` table with one column per model used in the session (e.g. Opus 4.6 / Opus 4.8 / Total) and rows for Input, Output, Cache read, Cache write, Cost. CLIde's `/usage` currently shows only aggregate session token counts and no money at all — **there is no cost data anywhere in the repo** (no `total_cost_usd`/`costUsd`; confirmed by grep 2026-07-27). Needs: (a) per-model aggregation of input/output/cache-read/cache-write from the transcript, which today is summed model-agnostically; (b) a pricing table — the SDK's embedded registry carries per-model prices (see memory `claude-model-registry-in-sdk`; grep `sdk.mjs`, never write prices from memory), which makes it a **second** table to refresh on every SDK bump alongside `CLAUDE_MODEL_CONTEXT_SPECS`. Provider-agnostic surface: Codex/Cursor/OpenCode report cost differently or not at all, so the breakdown must be capability-gated per adapter rather than assumed. Deferred 2026-07-27 when the ring/`/context` work shipped — the collapsible landed instead. **L**
- [ ] `!` shell mode in the conversation window (like the CLI). **M**
- [ ] Conversation "map" sidebar: a minimap showing where user/Claude messages sit in the convo (like doc editors/IDEs); tapping scrolls there. Depends on fixing the long-convo scroll jitter bug above. **L**
- [~] Double-tap Esc to stop a message mid-send and immediately begin editing it. **The stop-and-recover half landed 2026-07-27** (`e5ede32` + `adab285`, ADR 0013, see `docs/todo-done.md`): Stop is now signal-first so it works before a provider session id exists, and a turn cancelled before it reached the provider is retracted from the transcript with its text restored to the composer for editing or re-sending. **Remaining:** (a) the Esc/double-Esc keyboard gesture itself — today this is the Stop button only; (b) the harder case, editing a turn the provider *did* take (that's a rewind, not a retraction — see the transcript-rewind item). **M**
- [ ] `/rewind` command — and audit what other Claude Code CLI commands are missing from CloudCLI, integrate them. **L** *(the `/rewind` half shipped 2026-07-22 — see the transcript-rewind item below; the broader command audit stays open. Codex's CLI/SDK/App Server command and capability inventory is now recorded in `docs/maps/codex-cli-sdk-app-server.md`; implementation remains open.)*
- [~] Rewind via the transcript: scroll back to a message and hit "edit" to rewind there. **Phase A (conversation-only) SHIPPED + live-verified 2026-07-22; merged to `main` and pushed** (commits `daea812`…`845ed24`). Shape: per-message Edit pencil + `/rewind` picker → amber pending-rewind composer state → `chat.send` carries `rewindToMessageId` → server maps it to an assistant-uuid anchor → SDK `resume` + `resumeSessionAt`. `enableFileCheckpointing` is on (opt-out `CLIDE_DISABLE_CHECKPOINTS=1`) so checkpoints accumulate for Phase B. Design records: **ADR 0007** (rewind is in-place tree-append; `filterToActiveBranch` also fixes terminal-CLI-rewound sessions rendering abandoned turns — upstream candidate) + **ADR 0008** (`session_provider_aliases` tombstones stop first-message rewinds duplicating sidebar sessions, `8d4b8b6`); empirical SDK probe committed at `scripts/verify-rewind-sdk.ts`. **Codex conversation-only rewind and explicit forks implemented in `6e0954b` (ADR 0012); core App Server behavior live-verified 2026-07-25, with prior-message fork selection and CLIde-created `Fork:` titles live-verified 2026-07-26:** rewind, fork, Plan, send/abort, and Settings readiness all passed on the default transport. `/rewind` + the user-message pencil fork before the selected Codex turn and move the same stable CLIde session to the child provider thread; `/fork` now offers the same prior-message picker and preserves the parent while allocating a separate CLIde session through the selected completed turn. Forked session titles start with `Fork:`. The implementation avoids deprecated `thread/rollback`, updates the child transcript path with the provider-id remap, and labels disk-discovered `forked_from_id` children as forks rather than unrelated duplicates. Controls are capability-gated to `CLIDE_CODEX_CHAT_TRANSPORT=app-server`; SDK-mode Codex and Cursor/OpenCode do not advertise them. **Remaining:** (1) live Codex App Server verification: (a) mid-session pencil edit → tail drops + stays dropped after refresh; (b) first-message edit retains one stable sidebar session; (c) shell prompt-edit child appears with `Fork:` lineage; (d) SDK mode hides Codex rewind/fork/pencil. (2) Existing Claude verification checklist: terminal-CLI-rewound branch filtering + amber ring. (3) **Phase B — file restore via `rewindFiles`** (needs streaming-input turn). (4) Leftover duplicate row "Say hi" (30d43fd4) safe to delete in UI. **L**
- [ ] Modern IDE-style features: "@"-ing files, highlighting text in the editor to reference it in chat, "following" Claude's edits in realtime. **L**
- [ ] More IDE-like desktop layout: more split panels (convo, files, and editor open at the same time). **L**
- [ ] Landing page on entry. Currently just a blank page saying to tap the menu button for the sidebar. A list of recent convos would make sense — last-messaged time, token count, project, model… **M**
- [ ] **Scheduled messages.** When you run out of usage, you often want Claude to pick up where it left off the moment usage resets, especially mid-task. A "schedule send" option in the message box: set a time for the message to send. Also useful for follow-ups/checkups in threads later on. **M/L**
- [ ] **Background-session notifications — in-app banner + header roll-up dot, and stop the redundant OS notification while you're looking at a session.** Full design settled in `docs/specs/2026-07-21-background-session-notifications.md`: decision axis is app-visibility (3 states — A focused/this-session → nothing; B focused/other-session → in-app banner + header dot; C app not visible → OS push as already built), tiered by urgency (action-required = interruptive/amber/sticky; done = quiet dot). Header dot goes on the sidebar-toggle button (the mobile fix — sidebar overlay is closed by default so per-row status is hidden), amber-only for v1, count badge for colorblind-safety, attention-wins-over-done precedence. Transport: client already gets all sessions' frames over the single shared `/ws` (WebSocketContext dispatches every frame to all listeners) → build flavor (a) client-only listener first (filter `complete`/`permission_request` where sessionId≠active), then graduate to flavor (b) user-scoped orchestrator channel (mirror the existing desktop userId registry) to kill the reconnect blind spot. Also fix `sw.js:94` unconditional `showNotification` (focused-client check → postMessage vs OS notif). PWA-only behavior → final verify needs build + SSH restart on 3001, not the dev server. **L**
- [ ] **Self-hosted voice (STT + TTS) on the Pi — no app-code work, a deploy task.** CLIde already ships the whole voice feature (proxy `server/voice-proxy.js`, `VoiceSettingsTab`, `useVoiceInput`/`useTts`/`voicePlayer`, mic button + `MessageSpeakControl`) — it's a thin relay to any OpenAI-compatible audio backend. Plan settled in `docs/specs/2026-07-21-self-hosted-voice-piper-whisper.md`: stand up **one Flask shim** (`~/voice/`, systemd user service on `127.0.0.1:8890`) wrapping prebuilt **Piper** (TTS) + compiled **whisper.cpp** (STT, `base.en`, ffmpeg-transcode incoming webm) binaries — chosen over Docker (not installed, whole-daemon burden) and native pip (ctranslate2/onnxruntime wheel-rot risk on Py3.13) because binaries have no dependency tree. One shim serving both endpoints dissolves CLIde's single-base-URL constraint (no nginx unifier). Wire-in = one `VOICE_API_BASE_URL=http://127.0.0.1:8890` line in a `cloudcli.service.d/` drop-in (restart from SSH), client Settings fields left blank. All loopback → no ufw/Tailscale. ⚠️ never `apt install piper` (that's the GNOME gaming-mouse tool, not TTS). Go/no-go gates in the spec: whisper.cpp `make` compile + `base.en` latency on this CPU (fallbacks: `tiny.en` / warm `whisper-server`). **M — deploy/ops, not code**
- [x] ~~Voice Settings: the Base URL field in `VoiceSettingsTab` is dead~~ — **corrected 2026-07-29 while building Settings-IA P3a**: the field is not dead. It's true that the server proxy ignores it (`resolveConfig` in `voice-proxy.js` hardcodes `VOICE_API_BASE_URL`, a deliberate SSRF defense, and `voiceConfigHeaders()` never sends a client value), but `src/lib/voiceApi.ts` has a second path — when `baseUrl` is non-blank, it fetches the STT/TTS backend *directly from the browser*, bypassing the server proxy entirely. That's a real, working, already-documented bring-your-own-backend feature (`voiceSettings.note` i18n string explains the CORS requirement). Ported as-is, still editable, in `ChatVoiceBackendScreen.tsx`. No action needed; this entry was wrong.
- [~] **Interactive prompts never rendering / dead Stop / frozen "thinking" then a wall of missed messages** (observed 2026-07-18, recurred worse 2026-07-22). Root cause (code-verified): **undetected half-open WebSocket** — server pings were protocol-level (browsers answer them in the network stack, JS never sees them), the client had no liveness detection or connection UI, sends failed silently, and the server never reaped zombie sockets (the 2026-07-18 `AbortError: Stream closed` flavor). Full diagnosis + fix design: **ADR 0006**. All four parts implemented in `dd47ddd`: app-level `chat.ping`/`chat_pong` heartbeat + 10s watchdog forcing reconnect (also fixed a pre-existing double-connect race leaking a second live socket), `visibilitychange`/`online` probes, amber "Connection lost — reconnecting…" pill above the composer, `sendMessage` returns boolean + inline "Stop was not delivered" feedback, server pong-tracked `isAlive` sweep with `terminate()`. Provider-neutral (shared `/ws`) → [upstreamable, see WS pair in `docs/upstream-candidates.md`]. 15/15 websocket tests. **Verified live on the PWA (airplane-mode pill appeared + cleared) 2026-07-22. Still pending: desktop verify + network-tab ping/pong check — then move this and the replay item below to `docs/todo-done.md`.** **M**
- [x] **Replay-protocol holes: duplicate frames on re-subscribe + per-run/per-session seq mismatch → silent loss** (found 2026-07-22 chasing the long-observed "duplicate tool call, refresh fixes it" symptom). Two holes in the `chat.subscribe`/`seq` replay layer: (1) re-subscribing during a live run replayed frames the client already had — no seq drop-guard, blind store append → duplicates rendered until refresh; (2) server `seq` is per-run but the client counter never reset → a reconnect during a later run could **silently lose** in-flight frames. Fixed in `55d8c44` (+ comment cleanup `7ae4aa2`): runs tagged with a restart-safe `runId` stamped on every event + the `chat_subscribed` ack; runId mismatch → full replay; client dedup guard moved into `WebSocketContext` itself (drop before `dispatch()` — the sole path to all consumers, so delivery is exactly-once by construction; this also killed a third dup source, the per-component counter dying on navigation). 17/17 websocket tests. **Verified live 2026-07-22 after SSH restart (user-confirmed — no dups, reconnect replays missed frames).** Unexpected win: long-standing scroll jank while reading history is much smoother — phantom appends had been re-arming auto-scroll, poisoning the load-older scroll-restore delta, and double-dispatching re-renders; remaining streaming/idle jank is upstream #1050 (next item). Known trade-off: a stale pre-deploy tab full-replays with no drop guard until refreshed. [upstreamable — see WS pair in `docs/upstream-candidates.md`] *(Move to `docs/todo-done.md` together with the half-open-WS item above once its desktop check lands.)* **S/M**
- [ ] **Adopt upstream #1050's chat-scroll perf fixes (complementary to the `55d8c44` win).** Upstream issue #1050 (open, 2026-07-21, "Chat message-pane scrolling is choppy during streaming and while idle") documents three root causes that still exist in this fork after our exactly-once fix removed the phantom-append yanks: (1) `normalizedToChatMessages` mints brand-new `ChatMessage` objects for every message on every ~100ms stream flush, defeating `React.memo` on `MessageComponent` (note: our `ChatMessagesPane` key-map comment documents this same identity churn); (2) `Markdown`/`CodeBlock` unmemoized → ReactMarkdown re-parse + Prism re-highlight per message per flush; (3) the scroll handler is un-throttled, bound three ways (onWheel/onTouchMove/non-passive native), and does sync layout reads + setState per event — the *idle* jank. Issue body includes suggested fixes (identity-preserving conversion cache, memo wrappers, rAF-throttled passive listener). Watch for an upstream PR landing it; adopt locally if none appears. **M**

## Upstream candidates (PRs to siteboon/claudecodeui)

Moved to [`docs/upstream-candidates.md`](upstream-candidates.md) on 2026-07-27 — the
ledger of fixes tagged **upstreamable**, their upstream issue/PR checks, and PR status.
Tag new fixes there as upstreamable or personal-preference; nothing is PRed without
Grayson's explicit go-ahead.

## Done

Moved to [`docs/todo-done.md`](todo-done.md) on 2026-07-27 — the archive of completed,
verified work (what/why, commit, classification, verification). Append finished items there
and delete them from the sections above.
